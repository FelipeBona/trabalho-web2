package furb.trabalhoweb2;

// A anotação personalizada @EnableSwagger2 é definida aqui.
// Geralmente, isso seria usado para habilitar funcionalidades específicas,
// neste caso, pode ser usado para habilitar Swagger em uma aplicação Spring Boot.
public @interface EnableSwagger2 {
    // Não há métodos ou atributos definidos dentro desta anotação.
    // Esta é uma anotação de marcador (marker annotation), utilizada apenas para marcar uma classe ou método com um comportamento especial.
}

//Neste caso, a anotação @EnableSwagger2 é definida como uma anotação customizada. 
//Em projetos Spring Boot, geralmente há uma anotação pré-definida @EnableSwagger2 que é parte da biblioteca springfox-swagger2, 
//usada para habilitar o Swagger. A anotação definida aqui pode ser uma criação personalizada para fins específicos dentro do seu projeto.